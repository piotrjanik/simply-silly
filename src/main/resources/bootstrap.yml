micronaut:
  application:
    name: simplySilly
  config-client:
    enabled: true
kubernetes:
  client:
    discovery:
      mode: endpoint
      mode-configuration:
        endpoint:
          watch:
            enabled: true
